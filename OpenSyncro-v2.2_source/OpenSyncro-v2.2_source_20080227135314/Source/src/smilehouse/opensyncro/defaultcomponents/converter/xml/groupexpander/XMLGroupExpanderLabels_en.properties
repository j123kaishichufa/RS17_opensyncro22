component_description=Expands the input XML so that in the output there's a one parent element for every child element it contained.

parent_element=Parent element
grouping_element=Grouping element (the child elements must be grouped inside this element)
group_element=Child element
retain_grouping_elements=Retain grouping elements